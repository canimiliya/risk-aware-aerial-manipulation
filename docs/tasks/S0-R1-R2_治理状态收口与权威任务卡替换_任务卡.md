# S0-R1-R2：治理状态收口、权威任务卡替换与 S0 最终提交

## 一、任务定位

你是一名负责 GitHub 工程治理、科研项目状态维护、证据校验和最终提交收口的执行 Agent。

当前不是重新执行 S0 硬件审计，也不是进入 S1。S0-R1 和 S0-R1-R1 已完成主体工作，但第二次高级总控审查确认，PR #1 仍有以下残留问题：

1. 当前进度总控 v1.2 内部同时保留“项目尚未启动”和“S0 已提交审查”两套互相冲突的状态；
2. 仓库中的 S0-R1 任务卡不是高级总控最初生成的权威原件；
3. AM-Planner 许可证状态描述不准确；
4. PR #1 正文仍停留在第一次提交状态；
5. 当前验收脚本仍把任务卡哈希不一致视为 WARN，而不是硬失败。

本任务是 S0 的最后一次小范围返修。只允许修复治理文档、权威任务卡、许可证状态、检查脚本、测试证据和 PR 描述。

---

## 二、唯一目标

在原执行分支和原 Draft PR #1 中完成 S0 最终收口，使以下条件同时成立：

```text
当前进度总控内部无冲突
+
权威 S0-R1 任务卡 SHA-256 完全一致
+
AM-Planner 许可证表述符合冻结 commit 证据
+
自动检查 errors=0 且 warnings=0
+
PR 正文反映最新任务和最终提交
+
所有成果仍为 SUBMITTED_FOR_REVIEW
```

完成后只能申请高级总控审查，不得自行宣布 S0 通过。

---

## 三、固定 Git 与工作区上下文

### 项目根目录

```text
D:\Desktop\my_project\Simulation_Research_on_Aerial_Manipulator_for_Power_Line_Bird_Diverter_Operation
```

### GitHub 仓库

```text
https://github.com/canimiliya/risk-aware-aerial-manipulation.git
```

### 固定分支和 PR

```text
分支：agent/s0-r1-workspace-hardware-audit
PR：#1
PR 状态：Draft
main 基线：eba99e552877cb97e969734955ade7111f598032
本任务开始 HEAD：74a9ac5dde5c7b3ff7a34e9876582d03f224ad5e
```

### 强制要求

1. 必须继续使用现有分支；
2. 必须继续使用 PR #1；
3. 不得新建 S1 分支；
4. 不得 rebase、force push 或改写既有历史；
5. 开始前必须确认本地 HEAD 与远程分支一致；
6. 若 HEAD 已不是上述提交，先停止并报告，不得盲目覆盖新改动。

建议开始命令：

```powershell
Set-Location "D:\Desktop\my_project\Simulation_Research_on_Aerial_Manipulator_for_Power_Line_Bird_Diverter_Operation"

git status --short
git branch --show-current
git rev-parse HEAD
git remote get-url origin
git fetch origin
git rev-parse origin/agent/s0-r1-workspace-hardware-audit
```

预期：

```text
当前分支：agent/s0-r1-workspace-hardware-audit
本地 HEAD：74a9ac5dde5c7b3ff7a34e9876582d03f224ad5e
远程分支 HEAD：74a9ac5dde5c7b3ff7a34e9876582d03f224ad5e
工作树：干净
```

---

## 四、随附权威文件

本任务应与以下文件一同交付：

```text
S0-R1_项目初始化硬件与依赖审计_任务卡.md
```

该文件是高级总控最初生成的权威原件，其二进制基准固定为：

```text
字节数：27587
逻辑行数：1056
SHA-256：
3EA65A31E1D641847FAD1163D2A801B681B76CF9B49C3610C155C97CFD362425
```

### 关键原则

- 不得从聊天记录重新拼接；
- 不得复制当前仓库中的旧副本；
- 不得规范化换行后再声称二进制哈希一致；
- 必须使用本任务随附的权威原始文件；
- 必须先验证源文件哈希，再覆盖仓库目标文件；
- 若随附文件哈希不符合上述值，立即停止并报告 `BLOCKED_AUTHORITATIVE_TASK_FILE_MISMATCH`。

---

## 五、允许修改范围

只允许修改或新增：

```text
01_空中机械臂驱鸟器仿真研究项目_进度总控_v1.2.md
docs/tasks/S0-R1_项目初始化硬件与依赖审计_任务卡.md
docs/tasks/S0-R1-R2_治理状态收口与权威任务卡替换_任务卡.md
docs/reviews/S0-R1-R1_review_2026-07-31.md
docs/reports/S0-R1-R2_closeout_report.md
docs/milestones/S0_status.md
docs/decision_log.md
docs/third_party_manifest.md
third_party/licenses/AM-Planner_LICENSE_STATUS_7ea9a0a.md
scripts/audit/check_s0_structure.py
docs/evidence/S0-R1-R2/*
PR #1 的标题和正文
```

如确有必要，可对 README 中与 S0 当前状态直接冲突的文字做最小修改，但必须在报告中说明。

---

## 六、禁止事项

本轮禁止：

1. 安装 AM-Planner；
2. 安装 ROS；
3. 安装 Polynomial_DiT 或下载 checkpoint；
4. 安装 CUDA Toolkit；
5. 安装 Isaac Sim 或 Isaac Lab；
6. 修改 NVIDIA 驱动；
7. 修改 WSL、发行版、注册表或 Long Paths；
8. 运行规划复现；
9. 运行强化学习；
10. 创建 S1 分支；
11. 合并 PR #1；
12. 把 S0 写为 `PASS` 或 `PASS_WITH_LIMITATIONS`；
13. 删除 S0-R1 和 S0-R1-R1 的失败、返修与限制记录；
14. 修改权威总纲 v1.0；
15. 修改归档的进度总控 v1.0；
16. 为获得通过而伪造文件哈希或删除告警逻辑。

---

# 第一部分：权威任务卡替换

## 七、验证随附权威文件

假设随附权威文件位于下载目录或 Agent 可读取的附件路径。先定位该文件，禁止直接覆盖。

使用 Python 进行二进制核验：

```powershell
python -c "from pathlib import Path; import hashlib; p=Path(r'<权威附件绝对路径>'); b=p.read_bytes(); print('bytes=',len(b)); print('lines=',b.count(b'\n')+(0 if b.endswith(b'\n') else 1)); print('sha256=',hashlib.sha256(b).hexdigest().upper())"
```

必须得到：

```text
bytes=27587
lines=1056
sha256=3EA65A31E1D641847FAD1163D2A801B681B76CF9B49C3610C155C97CFD362425
```

任何字段不一致都不得继续替换。

## 八、二进制原样替换仓库任务卡

目标：

```text
docs/tasks/S0-R1_项目初始化硬件与依赖审计_任务卡.md
```

使用二进制复制，不能通过读取文本后重新写入：

```powershell
Copy-Item -LiteralPath "<权威附件绝对路径>" `
  -Destination "docs\tasks\S0-R1_项目初始化硬件与依赖审计_任务卡.md" `
  -Force
```

替换后再次运行同一哈希检查。

同时将本任务卡完整保存到：

```text
docs/tasks/S0-R1-R2_治理状态收口与权威任务卡替换_任务卡.md
```

仓库中保存的本任务卡必须是执行时实际收到的完整内容，不得用摘要替代。

---

# 第二部分：进度总控 v1.2 收口

## 九、修改原则

当前 v1.2 是以 v1.0 为主体恢复的完整文件，这一方向正确。但它保留了大量已经过时的字段，并在末尾追加“当前事实补充”，造成内部冲突。

本轮必须：

1. 保留完整的协作架构、状态词典、阶段表、审查模板和资源控制；
2. 直接更新原章节中的旧状态；
3. 将末尾补充事实整合进对应正式章节；
4. 删除不再需要的重复补丁章节；
5. 不得再次把 v1.2 缩减为简表。

## 十、文件头部

文件头至少应为：

```md
**版本：** v1.2
**当前状态：** `S0_SUBMITTED_FOR_REVIEW`
```

“建议文件名”必须更新为实际文件名：

```text
01_空中机械臂驱鸟器仿真研究项目_进度总控_v1.2.md
```

## 十一、仓库信息章节

将所有 `WAITING_INPUT` 更新为真实事实：

```text
仓库名称：risk-aware-aerial-manipulation
仓库 URL：https://github.com/canimiliya/risk-aware-aerial-manipulation.git
仓库所有者：canimiliya
可见性：Private
默认分支：main
远程名称：origin
是否已初始化 README：是
```

不得继续出现：

```text
GitHub 仓库：等待项目负责人创建
仓库 URL：WAITING_INPUT
仓库所有者：WAITING_INPUT
可见性：WAITING_INPUT
```

## 十二、本地工作区章节

写为：

```text
本地项目根目录：
D:\Desktop\my_project\Simulation_Research_on_Aerial_Manipulator_for_Power_Line_Bird_Diverter_Operation

是否已存在：是
是否属于其他 Git 仓库：否
父目录 Git 污染：未发现
初始化前旧文件：仅两份指定 Markdown
项目 Git 根：当前项目目录
主要可用磁盘：D 盘约 491 GB（以 S0-R1-R1 最新证据为准）
```

不得继续保留：

```text
本地项目根目录：WAITING_INPUT
是否已存在：WAITING_INPUT
是否属于其他 Git 仓库：NOT_VERIFIED
```

## 十三、硬件与系统章节

至少写入：

```text
CPU：Intel Core Ultra 7 270K Plus，24 核/24 逻辑线程
GPU：NVIDIA GeForce RTX 5060 Ti
显存：16,311 MiB
内存：50,873,458,688 bytes
操作系统：Windows 11 专业版，build 26100
NVIDIA 驱动：581.29
GPU compute capability：12.0
驱动 CUDA 兼容值：13.0
CUDA Toolkit：NOT_INSTALLED
LongPathsEnabled：0
WSL：发行版列表已记录；状态/版本探针超时
AirFAR-Ubuntu20：系统版本 NOT_VERIFIED
候选硬件分类：HARDWARE_PASS_WITH_LIMITATIONS
```

候选硬件分类不是阶段通过状态，必须注明：

```text
该分类只表示具备继续项目的候选硬件条件，不表示 Isaac Lab 已安装或 S0 已通过。
```

## 十四、当前总体状态章节

更新为类似：

```text
项目阶段：S0 项目初始化与硬件/依赖审计
总体状态：S0_SUBMITTED_FOR_REVIEW
权威总纲：PASS，v1.0
进度总控：v1.2
启动准备：PASS
GitHub 仓库：VERIFIED
本地工作区：VERIFIED
硬件候选分类：HARDWARE_PASS_WITH_LIMITATIONS
当前执行任务：S0-R1-R2
当前任务状态：SUBMITTED_FOR_REVIEW
S0：SUBMITTED_FOR_REVIEW
S1–S8：FROZEN
允许长训练：否
```

必须删除或替换：

```text
项目阶段：正式启动准备
总体状态：PRESTART_WAITING_REPOSITORY_AND_HARDWARE_INFO
当前执行任务：无
```

## 十五、阶段总表

至少更新：

| 阶段 | 当前状态 |
|---|---|
| 总纲 | `PASS` |
| 启动准备 | `PASS` |
| S0 | `SUBMITTED_FOR_REVIEW` |
| S1 | `FROZEN` |
| S2 | `FROZEN` |
| G-BRIDGE | `FROZEN` |
| S3–S8 | `FROZEN` |

不得再将 S0 写为 `NOT_STARTED`。

## 十六、当前任务槽位

更新为：

```text
任务编号：S0-R1-R2
任务名称：治理状态收口、权威任务卡替换与 S0 最终提交
任务状态：SUBMITTED_FOR_REVIEW
执行分支：agent/s0-r1-workspace-hardware-audit
基准 commit：74a9ac5dde5c7b3ff7a34e9876582d03f224ad5e
执行 Agent：GPT-5.6 Luna / Codex
开始时间：2026-07-31
最终 commit：通过聊天和 PR 报告提供
执行报告：docs/reports/S0-R1-R2_closeout_report.md
高级总控审查：等待第三次、最终 S0 审查
项目负责人决定：批准完成最后收口，不得进入 S1
```

## 十七、任务历史

保留：

```text
S0-R1 → REVISION_REQUIRED
S0-R1-R1 → REVISION_REQUIRED
S0-R1-R2 → SUBMITTED_FOR_REVIEW
```

必须记录两次高级总控审查均未批准 S0。

## 十八、当前待办

删除“创建仓库、提供路径和硬件”的旧待办。

替换为：

```text
高级总控从 GitHub 核验 S0-R1-R2 最终提交
→ 项目负责人决定是否接受 PASS_WITH_LIMITATIONS
→ 若批准，再合并 PR #1
→ 合并完成后才允许设计 S1 任务卡
```

## 十九、当前快照

至少应为：

```text
项目：空中机械臂驱鸟器仿真研究项目
日期：2026-07-31
总纲：PASS
进度总控：v1.2
GitHub：VERIFIED
本地工作区：VERIFIED
硬件候选条件：HARDWARE_PASS_WITH_LIMITATIONS
当前阶段：S0
当前任务：S0-R1-R2
任务状态：SUBMITTED_FOR_REVIEW
S0：SUBMITTED_FOR_REVIEW
S1：FROZEN
允许长训练：否
下一责任人：高级总控 AI
下一动作：GitHub 最终审查
```

## 二十、冲突字符串清理

在当前 v1.2 中，不得再出现以下已经失效的字符串：

```text
PRESTART_WAITING_REPOSITORY_AND_HARDWARE_INFO
GitHub 仓库：等待项目负责人创建
本地根目录：等待项目负责人提供
当前执行任务：无
仓库 URL：WAITING_INPUT
仓库所有者：WAITING_INPUT
可见性：WAITING_INPUT
本地项目根目录：WAITING_INPUT
CPU：WAITING_INPUT
GPU：WAITING_INPUT
操作系统：WAITING_INPUT
当前阶段：启动准备
允许执行 S0：尚未授权
下一动作：创建仓库
```

允许保留真正未知的事项，例如项目许可证最终决策、AirFAR 系统版本、Isaac Lab 启动能力，但必须用 `PENDING` 或 `NOT_VERIFIED`，而不是旧的启动等待状态。

---

# 第三部分：第二次审查记录

## 二十一、保存第二次高级总控审查

新增：

```text
docs/reviews/S0-R1-R1_review_2026-07-31.md
```

内容必须准确记录：

```text
审查对象：S0-R1-R1
审查 commit：74a9ac5dde5c7b3ff7a34e9876582d03f224ad5e
结论：REVISION_REQUIRED

已通过：
- 硬件和工具链证据可信；
- WSL 证据已转为 UTF-8；
- 自动检查明显增强；
- 候选硬件分类可接受。

仍需修复：
1. v1.2 内部保留旧 PRESTART/WAITING_INPUT/NOT_STARTED 状态；
2. S0-R1 权威任务卡哈希不一致；
3. AM-Planner 许可证说明不准确；
4. PR #1 正文未更新。

项目负责人决定：
批准执行 S0-R1-R2，继续使用原分支和 PR #1，不得进入 S1。
```

不要把高级总控结论改写成 Agent 自己的判断。

---

# 第四部分：AM-Planner 许可证状态

## 二十二、准确状态

冻结引用：

```text
仓库：SYSU-HILAB/am-planner
commit：7ea9a0a4c5a338efee1bf97c7f7e3e638e7d0d5d
```

已知证据：

1. 冻结 commit 的 README 第 7 节明确声明项目采用 MIT License；
2. README 链接到 `LICENSE`；
3. 但独立 `LICENSE` 文件在本轮现有核验中未成功取得。

因此状态必须写为：

```text
README_DECLARES_MIT_LICENSE_BUT_LICENSE_FILE_UNAVAILABLE_AT_FROZEN_COMMIT
```

不得再写：

```text
README 未给明确许可证
```

## 二十三、许可证状态文件

新增：

```text
third_party/licenses/AM-Planner_LICENSE_STATUS_7ea9a0a.md
```

至少记录：

```md
# AM-Planner 许可证状态

- 仓库：SYSU-HILAB/am-planner
- 冻结 commit：7ea9a0a4c5a338efee1bf97c7f7e3e638e7d0d5d
- README 声明：MIT License
- 独立 LICENSE 文件：本轮未成功取得
- 状态：README_DECLARES_MIT_LICENSE_BUT_LICENSE_FILE_UNAVAILABLE_AT_FROZEN_COMMIT
- 使用限制：S1 可进行本地科研复现；公开再分发前必须再次核验并保存独立许可证文件。
```

修改 `docs/third_party_manifest.md` 中对应条目，与上述状态完全一致。

Polynomial_DiT 继续保持：

```text
NO_EXPLICIT_LICENSE_FOUND_AT_FROZEN_COMMIT
```

Isaac Lab 继续保持：

```text
BSD-3-Clause
```

---

# 第五部分：强化自动检查

## 二十四、任务卡哈希改为硬失败

修改：

```text
scripts/audit/check_s0_structure.py
```

固定期望：

```python
EXPECTED_TASK_BYTES = 27587
EXPECTED_TASK_LINES = 1056
EXPECTED_TASK_SHA = "3EA65A31E1D641847FAD1163D2A801B681B76CF9B49C3610C155C97CFD362425"
```

必须直接读取二进制：

```python
task_bytes = task_path.read_bytes()
actual_bytes = len(task_bytes)
actual_lines = task_bytes.count(b"\n") + (0 if task_bytes.endswith(b"\n") else 1)
actual_sha = hashlib.sha256(task_bytes).hexdigest().upper()
```

三项中任何一项不一致，都加入 `errors`，不得再产生 WARN。

最终预期：

```text
errors=0
warnings=0
```

## 二十五、增加进度文件冲突检查

对当前 v1.2 检查以下禁用字符串不存在：

```python
FORBIDDEN_STALE_PROGRESS_MARKERS = [
    "PRESTART_WAITING_REPOSITORY_AND_HARDWARE_INFO",
    "GitHub 仓库：等待项目负责人创建",
    "本地根目录：等待项目负责人提供",
    "当前执行任务：无",
    "仓库 URL：WAITING_INPUT",
    "仓库所有者：WAITING_INPUT",
    "可见性：WAITING_INPUT",
    "本地项目根目录：WAITING_INPUT",
    "CPU：WAITING_INPUT",
    "GPU：WAITING_INPUT",
    "操作系统：WAITING_INPUT",
    "允许执行 S0：尚未授权",
]
```

同时检查当前事实存在：

```text
S0_SUBMITTED_FOR_REVIEW
S0-R1-R2
https://github.com/canimiliya/risk-aware-aerial-manipulation.git
D:\Desktop\my_project\Simulation_Research_on_Aerial_Manipulator_for_Power_Line_Bird_Diverter_Operation
HARDWARE_PASS_WITH_LIMITATIONS
```

## 二十六、增加许可证状态检查

检查：

```text
third_party/licenses/AM-Planner_LICENSE_STATUS_7ea9a0a.md
```

存在且包含：

```text
README_DECLARES_MIT_LICENSE_BUT_LICENSE_FILE_UNAVAILABLE_AT_FROZEN_COMMIT
```

检查 manifest 不包含：

```text
README 未给明确许可证
```

---

# 第六部分：状态页、决策日志与报告

## 二十七、S0 状态页

更新：

```text
docs/milestones/S0_status.md
```

当前必须是：

```text
S0-R1：REVISION_REQUIRED
S0-R1-R1：REVISION_REQUIRED
S0-R1-R2：SUBMITTED_FOR_REVIEW
S0 正式阶段状态：SUBMITTED_FOR_REVIEW
候选最终结论：PASS_WITH_LIMITATIONS
正式批准权：高级总控审查 + 项目负责人决策
S1：FROZEN
```

“候选最终结论”不等于批准。

## 二十八、决策日志

新增：

```text
D0021：高级总控第二次审查 S0-R1-R1，结论 REVISION_REQUIRED
D0022：项目负责人批准执行 S0-R1-R2 最终收口
D0023：权威 S0-R1 任务卡二进制基准固定为 27587 bytes、1056 lines、SHA-256 3EA65A31...
D0024：AM-Planner 许可证采用 README 声明与独立 LICENSE 文件可得性分离记录
```

状态均按事实写为 `ACTIVE`。

## 二十九、最终报告

新增：

```text
docs/reports/S0-R1-R2_closeout_report.md
```

必须包含：

1. 开始和最终 commit；
2. 替换前后任务卡哈希；
3. v1.2 中清理的旧状态；
4. AM-Planner 许可证新状态；
5. 检查脚本结果；
6. PR 正文是否更新；
7. 未执行的平台安装和训练；
8. 当前正式状态仍为 `SUBMITTED_FOR_REVIEW`；
9. 请求高级总控最终审查。

---

# 第七部分：测试与证据

## 三十、证据目录

创建：

```text
docs/evidence/S0-R1-R2/
```

至少包含：

```text
authoritative_task_hash.txt
progress_stale_marker_scan.txt
license_status_check.txt
structure_check.txt
git_diff_check.txt
git_state.txt
pr_snapshot.txt
```

## 三十一、必须运行的检查

### 1. 权威任务卡二进制检查

```powershell
python -c "from pathlib import Path; import hashlib; p=Path(r'docs/tasks/S0-R1_项目初始化硬件与依赖审计_任务卡.md'); b=p.read_bytes(); print('bytes=',len(b)); print('lines=',b.count(b'\n')+(0 if b.endswith(b'\n') else 1)); print('sha256=',hashlib.sha256(b).hexdigest().upper())"
```

必须得到目标三项。

### 2. 进度旧状态扫描

使用 Python 或 PowerShell 输出所有禁用标记的匹配结果。最终应为：

```text
STALE_PROGRESS_MARKERS=0
```

### 3. Python 编译

```powershell
python -m compileall scripts/audit
```

退出码必须为 0。

### 4. S0 结构检查

```powershell
python scripts/audit/check_s0_structure.py
```

必须：

```text
退出码：0
errors=0
warnings=0
```

### 5. Git 差异检查

```powershell
git diff --check
```

退出码必须为 0。

### 6. 大文件检查

```powershell
Get-ChildItem -Recurse -File |
    Where-Object Length -gt 10MB |
    Select-Object FullName, Length
```

不得新增超过 10 MB 文件。

### 7. 敏感信息检查

复用既有扫描方法，确保没有 token、私钥、密码或凭据。

### 8. Git 边界

```powershell
git rev-parse --show-toplevel
git remote get-url origin
git branch --show-current
git status --short
```

必须匹配固定项目、远程和分支。

---

# 第八部分：提交与 PR 更新

## 三十二、提交策略

建议使用两个提交。

### 提交 1：核心收口

```text
fix(s0): close governance and restore authoritative task
```

包含：

- 权威任务卡替换；
- v1.2 状态收口；
- 审查记录；
- 许可证状态；
- 检查脚本增强；
- 状态页和决策日志。

### 提交 2：证据与报告

```text
docs(s0): record final closeout evidence
```

包含：

- 测试证据；
- 最终报告；
- 最终一致性修正。

不得为了凑提交数量拆分无意义 commit。

## 三十三、推送

```powershell
git push origin agent/s0-r1-workspace-hardware-audit
```

禁止 force push。

## 三十四、更新 PR #1 正文

在最终 commit 推送后获取：

```powershell
git rev-parse HEAD
```

然后更新 PR #1 正文，使其至少包含：

```md
# S0 最终收口提交

任务：S0-R1-R2  
正式状态：`SUBMITTED_FOR_REVIEW`  
main 基线：`eba99e552877cb97e969734955ade7111f598032`  
最终 head：`<最新完整 SHA>`  

## 已完成

- 项目工作区和 Git 边界建立；
- 硬件、GPU、磁盘和工具链证据；
- 完整进度总控 v1.2；
- 权威 S0-R1 任务卡二进制恢复；
- WSL UTF-8 证据；
- 第三方引用和许可证状态；
- S0 自动检查：errors=0，warnings=0。

## 已知限制

- Isaac Sim/Lab 尚未安装和启动；
- AM-Planner、ROS、Polynomial_DiT 尚未安装；
- AirFAR-Ubuntu20 系统版本仍为 NOT_VERIFIED；
- LongPathsEnabled=0；
- 16 GB 显存后续需要 headless/小并行实测；
- Polynomial_DiT 无明确许可证，禁止公开再分发。

## 明确未执行

未进入 S1，未训练，未安装大型平台，未合并 PR。

## 请求

请求高级总控对最终 HEAD 进行 S0 最终审查。
```

PR 保持 Draft，不得标记 Ready for review，不得合并。

更新后通过：

```powershell
gh pr view 1 --json number,isDraft,state,headRefName,headRefOid,baseRefName,body,url
```

保存到：

```text
docs/evidence/S0-R1-R2/pr_snapshot.txt
```

---

# 第九部分：验收标准

## 三十五、全部通过条件

必须同时满足：

1. 本地和远程分支基于 `74a9ac5d...` 继续追加；
2. 权威任务卡为 27,587 bytes；
3. 权威任务卡为 1,056 行；
4. 权威任务卡 SHA-256 为 `3EA65A31...`;
5. v1.2 保留完整协作合同；
6. v1.2 不再包含旧 PRESTART/WAITING_INPUT 状态；
7. 当前任务为 S0-R1-R2；
8. S0 仍为 `SUBMITTED_FOR_REVIEW`；
9. S1–S8 仍为 `FROZEN`；
10. AM-Planner 状态准确区分 README 的 MIT 声明与独立 LICENSE 文件缺失；
11. Polynomial_DiT 仍标为无明确许可证；
12. 自动检查 `errors=0, warnings=0`；
13. 所有测试和退出码保存；
14. PR 正文已经更新；
15. PR 保持 Draft；
16. 未安装平台或开始训练；
17. 工作树最终干净；
18. 分支成功推送；
19. 最终报告与 GitHub 事实一致；
20. 未自行宣布 S0 通过。

---

## 三十六、允许的最终状态

### `SUBMITTED_S0_FINAL_CLOSEOUT`

全部条件满足，等待高级总控最终审查。

### `BLOCKED_AUTHORITATIVE_TASK_FILE_MISMATCH`

随附权威任务卡源文件自身不符合固定二进制基准。

### `REVISION_REQUIRED`

其他任一验收项未满足。

禁止使用 `PASS` 或 `PASS_WITH_LIMITATIONS`，因为批准权不属于执行 Agent。

---

# 第十部分：最终回报模板

```text
1. 最终执行状态
SUBMITTED_S0_FINAL_CLOSEOUT
或
BLOCKED_AUTHORITATIVE_TASK_FILE_MISMATCH
或
REVISION_REQUIRED

2. Git 事实
项目根目录：
分支：
开始 HEAD：
新增提交：
最终 HEAD：
远程分支 HEAD：
工作树：

3. 权威任务卡
源文件路径：
目标文件路径：
字节数：
逻辑行数：
SHA-256：
是否三项完全一致：

4. 进度总控收口
已清理的旧状态：
当前总体状态：
当前任务：
S0：
S1–S8：
是否保留完整协作合同：

5. 许可证
AM-Planner：
Isaac Lab：
Polynomial_DiT：
新增证据文件：

6. 自动检查
compileall：
check_s0_structure：
errors：
warnings：
git diff --check：
大文件：
敏感信息：

7. PR #1
URL：
Draft：
Base：
Head branch：
Head SHA：
正文是否更新：

8. 新增或修改的关键文件
<列出>

9. 明确未执行
确认未安装 AM-Planner、ROS、CUDA Toolkit、Isaac Sim/Lab；
确认未训练；
确认未进入 S1；
确认未合并 PR。

10. 已知限制
<列出仍需后续阶段解决的真实限制>

11. 请求
请求高级总控从 GitHub 对最终 HEAD 进行 S0 最终审查。
```

不得只返回摘要。所有结论必须与 GitHub 中的最终提交和证据一致。
