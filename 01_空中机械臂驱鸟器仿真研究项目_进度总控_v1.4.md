# 空中机械臂驱鸟器仿真研究项目：进度总控 v1.5

## S3 正式收口（2026-08-05）

- S0：`PASS_WITH_LIMITATIONS`；S1：`PASS_WITH_LIMITATIONS`；S2：`PASS_WITH_LIMITATIONS`；S3：`PASS_WITH_LIMITATIONS`。
- 正式关闭阶段：`4/9≈44%`。
- S3-R0：`CLOSED`；当前阶段：S4 尚未启动；S4–S8：`FROZEN`。
- S3 final acceptance：`docs/evidence/S3/final_acceptance/s3_final_acceptance.json`；PR #13 使用普通 merge 合并。
- 接受限制：kinematic playback articulation；无完整闭链动力学；provisional proxy / AABB envelope；距离对冻结 sampled-proxy 精确但不是 mesh-exact 安全证明；未做风、任务接触、闭环控制、ROS/ROS2 实时桥或训练；本地视频只提交 manifest、SHA 和元数据；保留 6 个既有 S0/S1/S2 历史审计/归档依赖测试失败。

## S2 最终收口候选（2026-08-04）

- 正式阶段：`3/9≈33%`；工程估算：约 `33%`。
- S2：`PASS_WITH_LIMITATIONS`（S2-R0～S2-R6 历史审计、无副作用回归、S2-R6 patch-only 复现和最终 acceptance 已完成）。
- S3：`NOT_STARTED`；S4--S8：`FROZEN`。本轮未启动任何后续研究阶段。
- 负责人需在 PR #10 完成普通合并后，继续按权限边界决定是否授权 S3。
- 证据：`docs/evidence/S2/final_readiness/s2_final_readiness.json`、`docs/reviews/S2_final_review_2026-08-04.md`、`docs/evidence/S2/final_acceptance/s2_final_acceptance.json`。
## R7-R3-R1 Python 3.9 overlay 与 GPU 轨迹验收（2026-08-03）

- S1-R1：`PASS_WITH_LIMITATIONS`（人话：官方 Python 3.9 ROS overlay、完整 ABI、19 包构建和 GPU write/grasp/lift/重复 grasp 证据已完成；保留三项已接受限制）。
- write、grasp、lift 和重复 grasp 均捕获 `/trajectory` 与 `/trajectory_arm`，NaN/Inf 为 0；详细告警和原始日志见 `docs/reports/S1-R1_r7r3_r1_python39_overlay_gpu_runtime_report.md`。
- S1 已收口为 `PASS_WITH_LIMITATIONS`，S2-R0 进入工作空间预检；S3--S8 仍为 `FROZEN`。

## R7 GPU 平行重建与 write 优先验收补充（历史状态 / 已解决阻断）

- S1-R1-R7：`BLOCKED_S1_R1_GPU_PYTHON_ABI`（人话：GPU 独立工作区 19/19 构建成功，但 `se3_node` 混链 Python 3.8/3.9，write 在启动时失败）。
- GPU 环境、固定源码和 CPU 基线证据已保存；write 未出双轨迹，所以 grasp/lift 和 grasp 重复性没有运行。
- 当时 S1 仍为 `IN_PROGRESS`，S2--S8 仍为 `FROZEN`；后续 ABI 授权与修复已在 R7-R3-R1 完成。
- 证据和报告：`docs/evidence/S1-R1/gpu_rebuild/`、`docs/reports/S1-R1_gpu_rebuild_and_runtime_report.md`。

## R6-R1 GPU 预检补充（历史状态 / 已解决阻断，2026-07-31）

官方 CUDA 依赖 21/21 已恢复并离线安装，RTX 5060 Ti `sm_120` kernel、原始权重、WorkspaceMLP strict forward/autograd 和关键 batch CPU/GPU 微基准均通过；GPU 路线判断为 `GPU_REBUILD_RECOMMENDED`。CPU 环境与固定 AM-Planner 源码未改变。本轮仍未重建 AM-Planner，未运行 grasp/write/lift；下一步需用户单独授权 GPU 并行重建。

## 当前状态（唯一当前状态区）

- S0：`PASS_WITH_LIMITATIONS`（基础治理已完成，但 Isaac Sim/Lab 尚未安装）。
- S1-R1：`PASS_WITH_LIMITATIONS`（人话：四次 GPU 双轨迹运行和三项告警的原始证据审查已完成；限制被接受，但不是零告警 PASS）。
- S1-R2：`PASS`（人话：真实 PolynomialTrajectory 合同、四组官方轨迹、waypoint 变体、采样验证、可视化和最终审计已通过）。
- S1：`PASS_WITH_LIMITATIONS`（人话：S1 正式收口，保留上游 Catkin、nvidia-smi 命令名和非致命发布日志限制）。
- S2：`PASS_WITH_LIMITATIONS`（人话：S2-R0～S2-R6 已完成最终证据收口；限制见最终 acceptance）。
- S3：`PASS_WITH_LIMITATIONS`；S4--S8：`FROZEN`，S4 尚未启动。
- R6 GPU 预检：`已解决`（人话：CUDA 依赖、kernel/模型探针和后续 GPU 运行已完成，原始预检记录仍保留）。

## 本轮证据

- CPU 权重根因与无源码修复：`docs/evidence/S1-R1/cpu_checkpoint_fix/`。
- R6 GPU 预检：`docs/reports/S1-R1_gpu_preflight_report.md`、`docs/evidence/S1-R1/gpu_preflight/`。
- 19/19 构建：`docs/evidence/S1-R1/build_after_torch/build_after_torch.log`。
- 三项运行证据：`docs/evidence/S1-R1/runtime/grasp_cpu_checkpoint_run_02/`、`write_cpu_checkpoint_run_01/`、`lift_cpu_checkpoint_run_01/`；write 诊断证据：`docs/evidence/S1-R1/write_diagnosis/` 和 `docs/evidence/S1-R1/write_diagnosis_run_01/`。

## 历史状态（已解决阻断）

旧的 write 性能定位授权等待和 R6 GPU 预检阻断已由后续授权记录与 R7-R3-R1 证据解决；本区仅保留历史语义，不作为当前状态。
