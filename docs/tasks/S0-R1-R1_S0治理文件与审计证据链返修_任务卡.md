S0-R1-R1：S0 治理文件与审计证据链返修

执行对象： GPT-5.6 Luna / Codex 低级执行 Agent项目阶段： S0——项目初始化与硬件/依赖审计任务性质： 原任务返修；不得进入 S1建议执行时长： 45～90 分钟GitHub 仓库： https://github.com/canimiliya/risk-aware-aerial-manipulation.git本地项目根目录： D:\Desktop\my_project\Simulation_Research_on_Aerial_Manipulator_for_Power_Line_Bird_Diverter_Operation继续使用分支： agent/s0-r1-workspace-hardware-audit继续使用 PR： #1main 基线： eba99e552877cb97e969734955ade7111f598032被审查的旧 HEAD： a7b4ff803322f4e8b8f5b91723250aa7b3603257高级总控审查结论： REVISION_REQUIRED

一、唯一目标

不要开始新的研究阶段。本轮只修复 S0-R1 已发现的治理与证据问题，使 PR #1 达到可再次审查的状态：

恢复仓库内完整、真实的原始 S0-R1 任务卡；

将根目录进度总控 v1.2 重建为完整权威状态文件，而不是 20 行摘要；

修复工具链、CUDA/计算能力、WSL 等审计证据与报告互相矛盾的问题；

将 WSL 原始输出转换为真正可读的 UTF-8 文本；

增强自动验收脚本，使其能发现本次暴露的问题；

重新核验三项第三方引用与许可证状态；

完善硬件审计、决策日志、风险登记和阶段状态；

在原分支追加一个或多个清晰提交，更新 Draft PR #1，等待高级总控第二次审查。

本轮完成后，正式任务状态仍只能是：

SUBMITTED_FOR_REVIEW

不得自行将 S0 写成 PASS 或 PASS_WITH_LIMITATIONS。

二、必须先读取的文件

执行前完整读取：

00_空中机械臂驱鸟器仿真研究项目_权威总纲_v1.0.md
01_空中机械臂驱鸟器仿真研究项目_进度总控_v1.2.md
docs/archive/01_空中机械臂驱鸟器仿真研究项目_进度总控_v1.0.md
docs/reports/S0-R1_execution_report.md
docs/evidence/S0-R1/*
docs/third_party_manifest.md
scripts/audit/collect_s0_hardware.ps1
scripts/audit/check_s0_structure.py

本任务卡末尾的“附录 A”是原始完整 S0-R1 任务卡的权威副本。

三、分支和工作区保护

3.1 继续原分支

不得新建 S1 分支，也不得从 main 重新开始。执行：

$ProjectRoot = 'D:\Desktop\my_project\Simulation_Research_on_Aerial_Manipulator_for_Power_Line_Bird_Diverter_Operation'
Set-Location $ProjectRoot

git status --short
git branch --show-current
git remote -v
git fetch origin
git switch agent/s0-r1-workspace-hardware-audit
git pull --ff-only origin agent/s0-r1-workspace-hardware-audit
git rev-parse HEAD

应确认开始 HEAD 为：

a7b4ff803322f4e8b8f5b91723250aa7b3603257

若远程分支已有后续提交，不得回退或强推；记录实际开始 HEAD，确认它是上述提交的后代后继续。若工作树不干净，先查明来源，不得直接丢弃。

3.2 禁止事项

本轮继续禁止：

安装或升级 AM-Planner、Polynomial_DiT、ROS、CUDA Toolkit、Isaac Sim、Isaac Lab、驱动、Docker、CMake 或 Ninja；

修改注册表、系统环境变量、WSL 功能和全局 Git 配置；

下载 checkpoint、大型资产或完整第三方工程；

训练、规划复现、GPU 压力测试；

修改或合并 main；

新建另一个 PR；

使用 git push --force；

删除旧失败证据来掩盖错误。

旧证据目录 docs/evidence/S0-R1/ 应保留，新的纠正证据写入 docs/evidence/S0-R1-R1/。

四、详细返修要求

4.1 恢复完整原始 S0-R1 任务卡

当前仓库中的任务卡只有数行摘要，不是实际执行依据。必须：

用本任务卡“附录 A”的完整内容，覆盖：

docs/tasks/S0-R1_项目初始化硬件与依赖审计_任务卡.md

文件必须为 UTF-8，正文不得删减、概括或改写；

附录 A 原始文件基准：

行数：1056
UTF-8 LF 字节数：27587
规范化 LF 后 SHA-256：3EA65A31E1D641847FAD1163D2A801B681B76CF9B49C3610C155C97CFD362425

校验时允许工作树受 Git 行尾转换影响，但将 CRLF 统一替换为 LF 后，SHA-256 必须等于上述值；

将本返修任务卡保存为：

docs/tasks/S0-R1-R1_S0治理文件与审计证据链返修_任务卡.md

建议新增 .gitattributes：

*.md text eol=lf
*.py text eol=lf
*.ps1 text eol=crlf
*.json text eol=lf
*.txt text eol=lf

不要因此批量重写无关文件的行尾。

4.2 重建完整进度总控 v1.2

当前根目录 v1.2 只有状态摘要，不能作为项目唯一进度总控。必须以归档的 v1.0 为主体，重建：

01_空中机械臂驱鸟器仿真研究项目_进度总控_v1.2.md

必须保留的原有结构

至少保留并更新以下内容，不得只放链接让后续 Agent 去归档文件查找：

文件权威性与使用规则；

项目负责人、高级总控 AI、低级执行 Agent、辅助解释工具的职责；

标准任务闭环和禁止流程；

GitHub 分支、提交和审查最低证据；

统一状态词典；

S0–S8 与 G-BRIDGE 阶段总表；

当前任务槽位；

历史任务表；

高级总控审查模板；

进度更新规则；

计算资源与长训练控制；

GitHub、本地路径、硬件与系统登记；

决策记录；

当前待办和进度快照。

v1.2 当前事实

必须写入以下事实：

GitHub：canimiliya/risk-aware-aerial-manipulation
URL：https://github.com/canimiliya/risk-aware-aerial-manipulation.git
可见性：Private
默认分支：main
本地根目录：D:\Desktop\my_project\Simulation_Research_on_Aerial_Manipulator_for_Power_Line_Bird_Diverter_Operation
main 基线：eba99e552877cb97e969734955ade7111f598032
执行分支：agent/s0-r1-workspace-hardware-audit
PR：#1，Draft，未合并
S0-R1 原提交：a7b4ff803322f4e8b8f5b91723250aa7b3603257
高级总控第一次审查：REVISION_REQUIRED
当前返修任务：S0-R1-R1
S0：SUBMITTED_FOR_REVIEW（返修完成后）
S1–S8：FROZEN
允许长训练：否

当前任务与历史

历史中保留 S0-R1：执行提交、执行报告、第一次审查结论 REVISION_REQUIRED；

当前任务槽位填写 S0-R1-R1；

不得使用状态词典之外的正式状态，例如 SUBMITTED_S0_WITH_LIMITATIONS；

可以在说明文字中写“预期若审查通过将属于有限通过”，但不能替代正式状态；

归档 v1.0 保持不变。

最低完整性

重建后的 v1.2 不得只是摘要。自动检查至少要求：

文件大于 12 KB；

存在关键章节标题；

包含角色权限、任务闭环、状态词典、阶段总表、当前任务、任务历史和硬件登记；

不存在把 S0 直接标成已批准 PASS 的文本。

4.3 新增高级总控第一次审查记录

创建：

docs/reviews/S0-R1_review_2026-07-31.md

内容至少包括：

审查对象：S0-R1
仓库：canimiliya/risk-aware-aerial-manipulation
PR：#1
基准：eba99e552877cb97e969734955ade7111f598032
被审查 HEAD：a7b4ff803322f4e8b8f5b91723250aa7b3603257
结论：REVISION_REQUIRED

并准确记录六个返修原因：

进度总控 v1.2 被缩减为摘要；

仓库任务卡不是完整原任务卡；

工具链报告与仓库证据矛盾；

WSL 证据编码损坏；

自动验收脚本过弱；

第三方许可证审计不完整。

不得把审查结论改写为已通过。

4.4 修复硬件和工具链采集脚本

修改：

scripts/audit/collect_s0_hardware.ps1

不要只修改文档，要让脚本真实生成新证据。新输出目录：

docs/evidence/S0-R1-R1/

4.4.1 版本命令必须真实执行

必须逐项采集命令、首要输出、退出码和可执行文件路径：

python --version
conda --version
git --version
gh --version
cmake --version
ninja --version
docker --version
nvcc --version

实现要求：

命令不存在时记录 NOT_INSTALLED；

命令存在但执行失败时记录 COMMAND_FAILED 和退出码；

不得把 usage: 或帮助页当成版本；

Python 的版本可能输出到 stderr，脚本必须正确捕获；

JSON 中为每项保存：status、path、version_output、exit_code；

文档中只能引用 JSON 和原始日志真实支持的版本。

4.4.2 正确区分 GPU 字段

至少保存：

nvidia-smi 完整顶部摘要；

查询字段：

name
memory.total
driver_version
compute_cap

必须明确区分：

compute_cap = 12.0：GPU 计算能力；

nvidia-smi 顶部 CUDA Version：驱动可支持的最高 CUDA runtime 兼容版本；

nvcc --version：本机 CUDA Toolkit 状态。

禁止再把 compute_cap 12.0 写成“驱动 CUDA 兼容版本”。

4.4.3 保存原始命令日志

至少创建：

docs/evidence/S0-R1-R1/hardware_audit.json
docs/evidence/S0-R1-R1/toolchain_summary.txt
docs/evidence/S0-R1-R1/toolchain_raw.txt
docs/evidence/S0-R1-R1/nvidia_smi.txt
docs/evidence/S0-R1-R1/path_and_disk_summary.txt
docs/evidence/S0-R1-R1/collection_log.txt

collection_log.txt 必须记录每条命令及退出码。

4.5 修复 WSL 证据编码并核验发行版

旧 docs/evidence/S0-R1/wsl_summary.txt 保留，不删除。新建可读 UTF-8 证据：

docs/evidence/S0-R1-R1/wsl_status_utf8.txt
docs/evidence/S0-R1-R1/wsl_version_utf8.txt
docs/evidence/S0-R1-R1/wsl_list_verbose_utf8.txt
docs/evidence/S0-R1-R1/AirFAR-Ubuntu20_os_release.txt

至少执行：

wsl --status
wsl --version
wsl --list --verbose
wsl -d AirFAR-Ubuntu20 -- cat /etc/os-release

编码要求

GitHub 打开上述文件时不得出现 NUL 字符或 UTF-16 乱码。推荐方案：

使用 cmd.exe /u /c 将 wsl.exe 输出写入临时 UTF-16LE 文件；

PowerShell 用 -Encoding Unicode 读取；

使用 .NET UTF8Encoding($false) 写成无 BOM UTF-8；

自动检查文件不含 0x00。

若 AirFAR-Ubuntu20 无法启动或不是 Ubuntu 20.04，只读记录真实结果，不得安装、升级或修改它。

在 docs/environment_lock.md 中根据 /etc/os-release 更新：

它是否可作为 S1 候选；

即便系统版本合适，也不得把“发行版存在”写成“ROS Noetic 环境已就绪”。

4.6 重新核验第三方引用与许可证

保持冻结引用：

AM-Planner：7ea9a0a4c5a338efee1bf97c7f7e3e638e7d0d5d
Isaac Lab v2.3.2：37ddf626871758333d6ed89cf64ad702aef127d0
Polynomial_DiT：f31c8f04e5fa045bc08c7bbaaadfe60bb54816f1

不得克隆完整仓库。允许通过 GitHub raw/contents、git ls-remote 或浅层只读方式下载少量许可证文本。

AM-Planner

在冻结 commit 上核验根目录许可证文件和 README 的许可证声明；

若确认 MIT，状态写为 VERIFIED_MIT_AT_FROZEN_COMMIT；

保存许可证文本到：

third_party/licenses/am-planner_LICENSE_7ea9a0a.txt

并记录 SHA-256 和来源 URL/commit。

Isaac Lab

复核 v2.3.2 许可证；

保存到：

third_party/licenses/isaaclab_LICENSE_v2.3.2.txt

记录 SHA-256，不得只保留表格结论。

Polynomial_DiT

检查冻结 commit 根目录和 README 是否存在明确许可证；

若确实没有，不得写成“MIT”或宽泛的 NOT_VERIFIED，应写：

NO_EXPLICIT_LICENSE_FOUND_AT_FROZEN_COMMIT

创建：

third_party/licenses/Polynomial_DiT_LICENSE_STATUS_f31c8f0.md

记录检查位置、冻结 commit、结果和风险：在许可澄清前，不把其源码或 checkpoint 重新分发到公开仓库。

更新：

docs/third_party_manifest.md
docs/risk_register.md

不得因许可证不明确就擅自取消 AM-Planner 主线；此处只记录研究和再分发风险。

4.7 完善硬件、环境、决策和风险文档

docs/hardware_audit.md

至少包含结构化表格：

OS、CPU、RAM；

GPU、显存、驱动；

compute capability；

驱动 CUDA 兼容值；

CUDA Toolkit 状态；

C/D 盘总量和剩余；

LongPathsEnabled；

WSL 与发行版；

Python/Conda/Git/gh/CMake/Ninja/Docker；

证据路径；

未验证项。

最终只能给出：

HARDWARE_PASS_WITH_LIMITATIONS

作为执行 Agent 的候选判断，并明确“最终阶段通过权仍属于高级总控”。限制至少包括：

Isaac Lab 未真实安装启动；

16 GB 显存需要 headless/小并行策略；

RTX 50 系组合仍需实测；

Long Paths 未启用；

AM-Planner/ROS 环境尚未安装；

C 盘容量需避免存放大型缓存，优先规划 D 盘缓存路径，但本轮不修改环境。

docs/environment_lock.md

写明两套环境严格隔离、离线接口、候选 OS/版本和当前真实状态；不得把候选方案写成已经可运行。

docs/decision_log.md

不得再把 D0001–D0011 压成一行。逐条恢复/记录至少 D0001–D0020，包含日期、状态、依据、影响和重评条件。新增：

D0019：高级总控对 S0-R1 的结论为 REVISION_REQUIRED
D0020：项目负责人批准在原分支和 PR #1 执行 S0-R1-R1 返修

docs/risk_register.md

一项风险一行，至少分别记录：

RTX 50/Isaac 兼容性；

16 GB 显存容量；

C 盘容量与缓存；

Long Paths；

Ubuntu 20.04/ROS Noetic；

AM-Planner 依赖；

Polynomial_DiT 无明确许可证；

Delta 臂进入 PhysX；

AM-Planner 与 Isaac 模型一致性；

接触数值稳定性；

项目范围膨胀。

每项必须有概率、影响、证据、触发条件、缓解措施和责任阶段。

阶段状态

更新：

docs/milestones/S0_status.md

至少写明：

S0-R1 第一次提交；

第一次审查为 REVISION_REQUIRED；

当前返修 S0-R1-R1；

返修提交后状态 SUBMITTED_FOR_REVIEW；

S1 未授权；

未满足项和限制。

4.8 增强自动验收脚本

重写或显著增强：

scripts/audit/check_s0_structure.py

脚本必须以非零退出码阻止以下问题：

缺少权威总纲、完整进度总控、完整原始任务卡、返修任务卡或审查记录；

进度总控小于 12 KB或缺少关键章节；

原始任务卡规范化 LF SHA-256 不等于：

3EA65A31E1D641847FAD1163D2A801B681B76CF9B49C3610C155C97CFD362425

新 WSL 文本含 NUL 字符或无法按 UTF-8 解码；

hardware_audit.json 无法解析，或 Python/Conda/Git/gh 字段仍只是 usage: 帮助文本；

compute_cap 与 driver_cuda_compatibility 没有分开保存；

关键目录或许可证证据缺失；

根目录不等于固定项目路径；

Git remote 不是指定仓库；

当前分支不是指定执行分支；

项目目录的祖先存在另一个 .git；

项目内存在超过 10 MB 的非允许文件；

出现常见令牌、私钥或凭据模式；

进度文件把 S0 写成已经批准的 PASS 或 PASS_WITH_LIMITATIONS；

正式状态使用状态词典外的自造状态。

允许 CMake/Ninja/Docker/nvcc 为 NOT_INSTALLED，但必须有真实查询结果和退出状态。

脚本应打印每项检查的 PASS/FAIL，不能只输出一句“structure passed”。

4.9 保存测试和返修报告

创建：

docs/reports/S0-R1-R1_repair_report.md
docs/evidence/S0-R1-R1/test_results.txt

保留原 docs/reports/S0-R1_execution_report.md，不要删除。新返修报告必须区分：

旧报告中的不一致；

本轮如何修复；

新证据支持的真实结论；

仍未安装和未验证的事项。

正式状态使用：

SUBMITTED_FOR_REVIEW

不得使用自造状态。

五、必须运行的验证

至少运行并把命令、输出摘要和退出码写入 test_results.txt：

# 重新采集
powershell -ExecutionPolicy Bypass -File scripts/audit/collect_s0_hardware.ps1

# Python 语法
python -m compileall scripts/audit

# 强化结构验收
python scripts/audit/check_s0_structure.py

# JSON 可解析
python -c "import json, pathlib; json.loads(pathlib.Path('docs/evidence/S0-R1-R1/hardware_audit.json').read_text(encoding='utf-8-sig')); print('JSON_OK')"

# NUL 字节扫描
python -c "from pathlib import Path; fs=list(Path('docs/evidence/S0-R1-R1').glob('*')); bad=[str(p) for p in fs if p.is_file() and b'\\x00' in p.read_bytes()]; print('NUL_OK' if not bad else bad); raise SystemExit(bool(bad))"

# Markdown/原任务卡哈希校验，应集成进主检查脚本
python scripts/audit/check_s0_structure.py

# Git 检查
git diff --check
git status --short
git diff --stat eba99e552877cb97e969734955ade7111f598032..HEAD

还要运行：

大于 10 MB 文件扫描；

常见密钥/令牌模式扫描；

git rev-parse --show-toplevel；

git remote get-url origin；

祖先目录 .git 检查；

完整原始任务卡规范化 SHA-256 校验。

任何检查失败，先修复再提交；不得把失败状态改写为成功。

六、Git 与 PR 要求

修复完成后：

git add --all
git diff --cached --check
git status --short
git commit -m "fix(s0): repair audit evidence and governance records"
git push origin agent/s0-r1-workspace-hardware-audit

必要时允许拆成两个逻辑提交，例如：

fix(s0): repair audit evidence collection
docs(s0): restore governance and review records

禁止 amend 已经提交并进入审查的旧提交，禁止强推。

更新 Draft PR #1 的说明或追加评论，写明：

本轮是 S0-R1-R1 返修；

新最终 commit；

六项问题逐项修复结果；

测试命令与退出码；

仍未安装任何大型依赖；

请求高级总控第二次审查。

保持 PR 为 Draft，不得合并，不得标记 ready for review，除非项目负责人另行明确要求。

七、验收标准

本轮只有同时满足以下条件，才允许提交第二次审查：

原始完整 S0-R1 任务卡已恢复并通过规范化 SHA-256；

返修任务卡已保存；

根目录 v1.2 是完整进度总控，不是摘要；

高级总控第一次审查记录已保存；

新工具链证据和报告一致；

Python、Conda、Git、gh 的版本证据真实可读；

compute capability、驱动 CUDA 兼容值和 CUDA Toolkit 状态被正确区分；

新 WSL 证据为可读 UTF-8 且无 NUL；

AirFAR-Ubuntu20 的真实系统版本已只读核验；

AM-Planner 和 Isaac Lab 许可证有冻结 commit 对应文本与哈希；

Polynomial_DiT 无明确许可证时使用准确状态；

硬件候选判断为 HARDWARE_PASS_WITH_LIMITATIONS，但 S0 正式状态仍为 SUBMITTED_FOR_REVIEW；

决策日志逐项记录，不再合并 D0001–D0011；

风险登记一项一行，字段完整；

强化检查脚本可以识别本轮旧问题；

所有要求的检查退出码为 0；

没有大型二进制、凭据和隐私信息；

没有安装平台、修改系统或进入 S1；

新提交已推送到原分支和原 PR；

最终工作树干净。

任一核心项无法满足，正式状态应保持 REVISION_REQUIRED 或报告具体 BLOCKED，不得自称通过。

八、最终回报模板

必须严格按以下结构返回：

1. 最终执行状态
SUBMITTED_FOR_REVIEW
或
REVISION_REQUIRED
或
BLOCKED

2. Git 事实
项目根目录：
分支：agent/s0-r1-workspace-hardware-audit
main 基线：eba99e552877cb97e969734955ade7111f598032
开始 HEAD：
最终 HEAD：
新增提交：
PR #1 状态：
工作树状态：

3. 六项返修结果
进度总控：
完整任务卡：
工具链证据：
WSL 编码：
自动验收：
许可证审计：

4. 重新核验的硬件和工具链
OS：
CPU：
RAM：
GPU/VRAM：
驱动：
compute capability：
驱动 CUDA 兼容值：
CUDA Toolkit：
Python：
Conda：
Git：
gh：
CMake：
Ninja：
Docker：
Long Paths：

5. WSL
wsl --status：
wsl --version：
发行版：
AirFAR-Ubuntu20 /etc/os-release：
AM-Planner 候选结论：

6. 第三方引用与许可证
AM-Planner：
Isaac Lab：
Polynomial_DiT：
许可证证据路径：

7. 关键文档
进度总控：
第一次审查记录：
硬件审计：
第三方清单：
风险登记：
返修报告：

8. 测试
逐条列出命令、退出码和结果，不得只写“全部通过”。

9. 仍存在的限制

10. 明确未执行
确认未安装 AM-Planner、ROS、CUDA Toolkit、Isaac Sim/Lab，未训练，未修改注册表/驱动/WSL，未合并 PR。

11. 请求
请求高级总控从 GitHub 对 PR #1 新 HEAD 进行第二次审查。

附录 A：原始完整 S0-R1 任务卡权威副本

以下内容必须原样恢复到 docs/tasks/S0-R1_项目初始化硬件与依赖审计_任务卡.md。不要把附录标题本身复制进去；从下一行 # S0-R1：... 开始复制到文…1535 tokens truncated…oplevel 返回的根目录不是本项目目录，而是其父目录；

当前 .git 指向其他项目或远程仓库；

权威总纲缺失、无法读取或内容明显损坏；

目录内存在来源不明的大量旧代码，无法确认是否属于本项目。

此时状态必须为 BLOCKED_S0_WORKSPACE，保留只读检查结果后停止。

Step 2：创建一次性最小 main 基线

仅在工作区安全时执行。

2.1 初始化或核验 Git

若尚无 Git：

git init -b main
git remote add origin https://github.com/canimiliya/risk-aware-aerial-manipulation.git

若已存在 Git，不得重新初始化；核验：

git status --short
git branch --show-current
git remote -v

远程 URL 不一致时停止并报告，禁止静默替换。

若 Git 身份未配置，只允许使用仓库局部配置，不得修改全局配置。优先沿用已有身份；确实为空时可使用：

git config user.name 'canimiliya'
git config user.email '132739466+canimiliya@users.noreply.github.com'

2.2 最小基线内容

首次 main commit 只能包含当前两份原始 Markdown 文件，不得混入后续生成文件：

git add -- '00_空中机械臂驱鸟器仿真研究项目_权威总纲_v1.0.md' `
           '01_空中机械臂驱鸟器仿真研究项目_进度总控_v1.0.md'
git diff --cached --check
git commit -m 'docs(init): add authoritative project documents'
git push -u origin main

记录此完整 40 位 commit 为：

BASE_COMMIT

这是空仓库的一次性 bootstrap 例外；后续不得直接在 main 开发。

Step 3：建立 S0 执行分支

git switch -c agent/s0-r1-workspace-hardware-audit

核验：

git branch --show-current
git rev-parse HEAD
git status --short

预期：

当前分支：agent/s0-r1-workspace-hardware-audit
HEAD：BASE_COMMIT
工作树：干净

Step 4：建立项目工程骨架

至少建立：

configs/
  planner/
  robot/
  scene/
  wind/
  contact/
  train/
  eval/
docs/
  archive/
  decision_log.md
  hardware_audit.md
  environment_lock.md
  research_contract.md
  trajectory_contract.md
  risk_register.md
  third_party_manifest.md
  milestones/S0_status.md
  tasks/
  reports/
  evidence/S0-R1/
third_party/
  README.md
  licenses/
planner_bridge/
  export/
  validation/
  tests/
aerial_manipulation/
  assets/
  envs/
  controllers/
  disturbances/
  contacts/
  safety/
  policies/
  encoders/
  rewards/
  utils/
scripts/
  audit/
  train/
  evaluate/
  reproduce/
  visualize/
tests/
  unit/
  integration/
  regression/
data/
  trajectories/
  evaluation/
  metadata/
outputs/
  checkpoints/
  logs/
  figures/
  tables/
  videos/
paper/
  outline.md
  figures/
  tables/
README.md
.gitignore

仅对确实需要在 Git 中保留的空目录使用 .gitkeep，不要制造大量无意义占位文件。

README 最低内容

项目中文名和英文仓库名；

一句话研究目标；

规划器—离线轨迹—Isaac Lab—控制与学习的主链；

明确当前只处于 S0；

明确尚未安装平台、尚未复现算法、尚无实验结果；

GitHub 协作角色和任务闭环；

目录说明；

后续阶段顺序。

不得写“已支持”“已实现”“成功率”等未经证据验证的内容。

项目许可证

本轮不得擅自选择项目 LICENSE。README 与决策日志中记录：

PROJECT_LICENSE_DECISION_PENDING

Step 5：建立 .gitignore

至少覆盖：

__pycache__/
*.py[cod]
.venv/
venv/
env/
.conda/
.vscode/
.idea/

outputs/checkpoints/*
outputs/logs/*
outputs/videos/*
data/evaluation/*
data/trajectories/*

*.pt
*.pth
*.ckpt
*.onnx
*.npz
*.bag
*.mp4

omniverse/
isaac-sim/
_cache/
logs/
wandb/
runs/

third_party/am-planner/
third_party/IsaacLab/
third_party/Polynomial_DiT/

.env
*.pem
*.key
*token*
*credentials*

为需要提交的 .gitkeep、README、manifest 和小型审计证据设置合理例外。不得忽略权威总纲、当前进度文件、配置、测试和文档。

Step 6：硬件、系统和工具链命令审计

创建：

scripts/audit/collect_s0_hardware.ps1

脚本必须只收集必要字段，不得导出完整系统清单或个人隐私。至少执行以下等价查询。

6.1 操作系统、CPU、内存

Get-CimInstance Win32_OperatingSystem |
  Select-Object Caption, Version, BuildNumber, OSArchitecture

Get-CimInstance Win32_Processor |
  Select-Object Name, NumberOfCores, NumberOfLogicalProcessors, MaxClockSpeed

Get-CimInstance Win32_ComputerSystem |
  Select-Object TotalPhysicalMemory

6.2 GPU、显存和驱动

nvidia-smi --query-gpu=name,memory.total,driver_version,compute_cap --format=csv,noheader
nvidia-smi

保存原始 nvidia-smi 前检查其中是否含不必要的进程路径。仓库证据优先保留查询模式结果和头部摘要，不提交完整进程列表。

另检查：

where.exe nvidia-smi
where.exe nvcc
nvcc --version

nvcc 不存在只能说明本地 CUDA Toolkit 未安装，不能直接判断 GPU 或 Isaac Lab 不可用。

6.3 磁盘

同时检查 C 盘和项目所在 D 盘：

Get-CimInstance Win32_LogicalDisk -Filter "DriveType=3" |
  Select-Object DeviceID, FileSystem, Size, FreeSpace

必须报告真实剩余空间，不能只报告“1 TB 总容量”。

6.4 WSL 与虚拟化线索

wsl --status
wsl --list --verbose
wsl --version

不得在本轮启用 WSL 或安装发行版。

6.5 Python、Conda、Git 与 GitHub CLI

where.exe python
python --version
py -0p

where.exe conda
conda --version
conda info
conda env list

git --version
git config --show-origin --get user.name
git config --show-origin --get user.email

where.exe gh
gh --version
gh auth status

gh auth status 只用于判断能否创建 PR，不得保存访问令牌或敏感认证内容。

6.6 Windows 长路径风险

项目根目录较长，必须只读检查：

reg query HKLM\SYSTEM\CurrentControlSet\Control\FileSystem /v LongPathsEnabled

不得修改注册表。若 LongPathsEnabled=0 或查询失败，在风险登记中记录，不得擅自改变用户给定根目录。

6.7 可选工具

只检查是否存在，不安装：

where.exe cmake
cmake --version
where.exe ninja
ninja --version
where.exe docker
docker --version

Step 7：形成结构化硬件证据

至少生成并提交以下脱敏文件：

docs/evidence/S0-R1/initial_file_hashes.txt
docs/evidence/S0-R1/hardware_audit.json
docs/evidence/S0-R1/hardware_summary.txt
docs/evidence/S0-R1/toolchain_summary.txt
docs/evidence/S0-R1/wsl_summary.txt
docs/evidence/S0-R1/path_and_disk_summary.txt

hardware_audit.json 至少包含：

{
  "audit_date": "2026-07-31",
  "os": {},
  "cpu": {},
  "memory_bytes": 0,
  "gpu": {},
  "disks": [],
  "wsl": {},
  "python": {},
  "conda": {},
  "git": {},
  "long_paths": {},
  "unknowns": []
}

禁止包含：

Windows 产品密钥；

BIOS/硬盘序列号；

MAC、IP 和网络配置；

GitHub token；

用户密码；

与项目无关的软件清单；

完整用户主目录内容。

Step 8：第三方远程引用与许可证审计

不得完整克隆仓库。使用：

git ls-remote https://github.com/SYSU-HILAB/am-planner.git HEAD refs/heads/main
git ls-remote https://github.com/isaac-sim/IsaacLab.git HEAD refs/heads/main refs/tags/v2.3.2 'refs/tags/v2.3.2^{}'
git ls-remote https://github.com/Dwl2021/Polynomial_DiT.git HEAD refs/heads/main refs/heads/master

将完整输出保存到：

docs/evidence/S0-R1/third_party_refs.txt

通过官方仓库网页或 Raw 文件检查常见许可证文件：

LICENSE
LICENSE.md
LICENSE.txt
COPYING

许可证必须写为实际核验结果；未找到时写 LICENSE_NOT_FOUND_OR_NOT_VERIFIED，不得根据总纲或记忆猜测。

在 docs/third_party_manifest.md 中至少记录：

项目名称
官方仓库
查询日期
默认分支
HEAD 完整 commit
目标 tag 与 tag 完整 commit
许可证文件路径、许可证名称和文件 SHA256
用途
是否已克隆
是否已安装
是否需要 checkpoint/资产
已知依赖风险

冻结原则：

Isaac Lab 候选版本：v2.3.2，必须记录完整 tag commit，不跟随 develop；

AM-Planner：记录执行日官方 main HEAD，S1 开始前再次复核后正式冻结；

Polynomial_DiT：记录官方 HEAD、许可证和 checkpoint 下载机制；

三者本轮均标记为 NOT_CLONED_IN_S0、NOT_INSTALLED_IN_S0。

Step 9：环境隔离合同

在 docs/environment_lock.md 中形成两个独立候选环境。

9.1 AM-Planner 候选环境

候选运行位置：WSL2 Ubuntu 20.04 或独立 Ubuntu 20.04 环境
ROS：Noetic
Python：3.8
CUDA：官方推荐 11.8，是否为本机硬依赖待 S1 实测
构建系统：Catkin
依赖：Polynomial_DiT
用途：官方示例复现、名义规划和离线轨迹导出
状态：NOT_INSTALLED

9.2 Isaac Lab 候选环境

候选运行位置：Windows 11 本机
Isaac Sim：5.1
Isaac Lab：v2.3.2
Python：3.11
用途：动力学、接触、风扰、闭环控制与强化学习
状态：NOT_INSTALLED

9.3 不可违反的隔离原则

不得把 AM-Planner 与 Isaac Lab 安装进同一个 Conda 环境。
不得为了连接两个平台建立实时 ROS 桥。
唯一正式接口为离线轨迹文件和校验器。
本轮不得创建上述大型环境。

必须根据实际 WSL、操作系统、驱动和磁盘证据判断环境方案是：

FEASIBLE_PENDING_INSTALLATION

FEASIBLE_WITH_LIMITATIONS

BLOCKED_BY_CURRENT_SYSTEM

Step 10：核心治理文档

10.1 docs/hardware_audit.md

必须包含：

截图线索与命令实测的区分；

CPU、GPU、VRAM、RAM、OS、驱动、磁盘、WSL；

Isaac Lab 基础条件逐项对照；

RTX 50 系显卡与驱动风险；

16 GB 显存处于候选平台基础门槛附近时的容量限制；

无相机、headless、小并行环境数等未来缓解方案；

最终硬件分类与证据路径。

10.2 docs/decision_log.md

至少记录：

D0001～D0011：从现有进度文件继承
D0012：硬件截图仅作为线索，S0 必须命令核验
D0013：授权执行 S0-R1
D0014：空仓库允许一次性创建最小 main 基线
D0015：后续实质修改必须使用 agent 分支
D0016：项目长路径风险只审计、不擅自迁移目录
D0017：S0 禁止安装大型依赖和训练
D0018：项目 LICENSE 暂不决定

每条包含日期、状态、依据、影响和重新评估条件。

10.3 docs/research_contract.md

至少冻结：

当前只做纯仿真；

不做视觉、SLAM、全局导航、PX4、真机通信；

AM-Planner 和 Isaac Lab 均不是论文创新；

两个平台仅使用离线轨迹接口；

未通过阶段门槛不得训练；

actor 不得读取真实风速、质量、摩擦、卡扣阈值和未来释放信息；

所有结果必须可追溯到配置、种子、日志和 commit。

10.4 docs/trajectory_contract.md

本轮仅建立协议草案，不实现转换器。至少列出总纲中的必需字段、单位、坐标系、四元数顺序待冻结项和验收检查。状态写：

DRAFT_NOT_IMPLEMENTED

10.5 docs/risk_register.md

至少记录：

GPU 显存与驱动风险；

C/D 盘空间风险；

项目路径过长；

WSL/Ubuntu/ROS Noetic 未准备；

AM-Planner 依赖和 checkpoint 风险；

Delta 臂闭链进入 PhysX 的风险；

规划器与 Isaac 机器人模型不一致；

第三方许可证不明；

接触仿真稳定性；

项目方法范围过大。

每项包含概率、影响、当前证据、缓解措施、触发条件和责任阶段。

Step 11：升级进度总控到 v1.2

将原始文件保持原样移动到：

docs/archive/01_空中机械臂驱鸟器仿真研究项目_进度总控_v1.0.md

在项目根目录创建：

01_空中机械臂驱鸟器仿真研究项目_进度总控_v1.2.md

v1.2 必须保留原文件的协作架构，并更新：

当前阶段：S0
总体状态：S0_SUBMITTED_FOR_REVIEW（任务完成提交时）
启动准备：PASS
S0：SUBMITTED_FOR_REVIEW
S1～S8：FROZEN
当前任务：S0-R1
执行分支：agent/s0-r1-workspace-hardware-audit
基准 commit：BASE_COMMIT
证据 commit：EVIDENCE_COMMIT
最终提交 commit：在聊天回报中提供，不在文件中制造自引用
允许长训练：否

更新硬件、仓库、本地路径、工具链和 WSL 的实测信息；

记录本轮新增决策与风险；

低级 Agent 无权把 S0 写成 PASS 或 PASS_WITH_LIMITATIONS。只有高级总控审查 GitHub 后才能修改为通过。

Step 12：审计脚本与自动检查

创建：

scripts/audit/check_s0_structure.py

脚本至少检查：

必需文件和目录存在；

根目录存在且路径与任务卡一致；

当前根目录只有一个 .git；

00_权威总纲 存在；

当前进度文件为 v1.2；

原 v1.0 进度文件已归档；

关键审计证据存在且非空；

JSON 可以解析；

进度文件包含 SUBMITTED_FOR_REVIEW，不包含把 S0 写为已批准 PASS 的错误状态；

无超过设定阈值的未知大文件。

缺失项必须打印清楚并返回非零退出码。

运行：

python .\scripts\audit\check_s0_structure.py
python -m compileall .\scripts\audit
git diff --check

若系统没有可用 Python，创建等价 PowerShell 检查器并将“Python 未准备”记录为限制，不得伪造 Python 测试通过。

Step 13：隐私、密钥和大文件检查

至少执行：

# 大文件审计
Get-ChildItem -Recurse -File |
  Where-Object { $_.Length -gt 10MB } |
  Select-Object FullName, Length

# Git 待提交文件
 git status --short
 git diff --check

# 基础敏感信息扫描；命令可按 Windows 工具实际情况调整
 git grep -n -I -E 'ghp_|github_pat_|AKIA|BEGIN (RSA |OPENSSH )?PRIVATE KEY|password\s*=|token\s*=' -- .

扫描命中必须人工判断；文档中普通文字“token”不等同于泄漏。不得为了通过而删除失败证据。

再次核验 00_权威总纲 SHA256 与 Step 1 初始哈希一致。

Step 14：提交策略

建议形成三个逻辑提交。

提交 A：工程骨架

git add .
git diff --cached --check
git commit -m 'chore(s0): initialize project workspace'

提交 B：硬件与依赖审计

git add .
git diff --cached --check
git commit -m 'docs(s0): record hardware and dependency audit'

记录提交 B 完整 SHA 为：

EVIDENCE_COMMIT

提交 C：提交审查状态

将 EVIDENCE_COMMIT 写入进度文件和执行报告，状态设置为 SUBMITTED_FOR_REVIEW，再提交：

git add .
git diff --cached --check
git commit -m 'docs(s0): submit initialization for review'

记录提交 C 完整 SHA 为：

SUBMISSION_COMMIT

推送：

git push -u origin agent/s0-r1-workspace-hardware-audit

不得合并到 main。

若 GitHub CLI 已认证，可创建 Draft PR：

gh pr create --draft `
  --base main `
  --head agent/s0-r1-workspace-hardware-audit `
  --title '[S0-R1] Project bootstrap and hardware audit' `
  --body-file '.\docs\reports\S0-R1_execution_report.md'

没有 gh 或未认证时不构成阻断，分支成功推送即可。

七、必须产出的文件

至少包括：

README.md
.gitignore
00_空中机械臂驱鸟器仿真研究项目_权威总纲_v1.0.md
01_空中机械臂驱鸟器仿真研究项目_进度总控_v1.2.md

docs/archive/01_空中机械臂驱鸟器仿真研究项目_进度总控_v1.0.md
docs/decision_log.md
docs/hardware_audit.md
docs/environment_lock.md
docs/research_contract.md
docs/trajectory_contract.md
docs/risk_register.md
docs/third_party_manifest.md
docs/milestones/S0_status.md
docs/tasks/S0-R1_项目初始化硬件与依赖审计_任务卡.md
docs/reports/S0-R1_execution_report.md

docs/evidence/S0-R1/initial_file_hashes.txt
docs/evidence/S0-R1/hardware_audit.json
docs/evidence/S0-R1/hardware_summary.txt
docs/evidence/S0-R1/toolchain_summary.txt
docs/evidence/S0-R1/wsl_summary.txt
docs/evidence/S0-R1/path_and_disk_summary.txt
docs/evidence/S0-R1/third_party_refs.txt

scripts/audit/collect_s0_hardware.ps1
scripts/audit/check_s0_structure.py
third_party/README.md
paper/outline.md

八、S0 验收标准

低级 Agent 完成提交时，必须满足：

项目目录被证明不属于其他 Git 仓库；

GitHub main 存在最小基线且只含两份原始权威文档；

S0 实质修改全部位于 agent/s0-r1-workspace-hardware-audit；

00_权威总纲 SHA256 前后相同；

CPU、GPU、VRAM、RAM、OS、驱动、C/D 盘剩余空间有真实命令证据；

WSL2 和现有 Linux 发行版状态已核验；

Python、Conda、Git、gh、CMake、Ninja 等工具状态已记录；

Windows 长路径设置已只读核验；

三个第三方官方仓库的远程引用和许可证已尽力核验；

两套环境明确隔离，且均未安装；

项目骨架、README、.gitignore 和治理文档完整；

结构检查脚本退出码为 0，或明确记录无法运行的真实限制；

没有提交 token、密码、序列号或大型二进制；

执行分支已推送 GitHub；

进度文件只写 SUBMITTED_FOR_REVIEW，不擅自判定 PASS；

没有开始 S1、没有安装大型平台、没有启动训练。

九、状态判定

最终执行报告只能使用以下状态之一：

SUBMITTED_S0_INITIALIZATION

全部核心目标完成，已推送 GitHub，等待高级总控审查。

SUBMITTED_S0_WITH_LIMITATIONS

工作区和 GitHub 初始化完成，但存在明确硬件、驱动、磁盘、WSL、工具链或第三方许可证限制。限制必须逐项说明，不得掩盖。

BLOCKED_S0_WORKSPACE

工作区嵌套、远程不一致、权限或原始文件问题导致无法安全初始化。

BLOCKED_S0_GIT_PUSH

本地工作与证据完成，但认证或网络导致无法推送。必须保留本地 commit 和错误日志，不得声称已经提交 GitHub。

BLOCKED_S0_HARDWARE_STRATEGY

硬件或操作系统存在明确硬阻断，且当前没有可信的本地或远程运行方案。

执行 Agent 不得使用 PASS；通过与否由高级总控审查决定。

十、失败与阻断处理

命令失败必须记录命令、时间、退出码和关键错误；

网络查询失败时可重试一次，仍失败则标记 NOT_VERIFIED_DUE_TO_NETWORK；

Git push 失败不得反复修改远程或使用强推；

发现敏感信息时先从暂存区移除并记录，不得将密钥提交后再删除；

发现路径或仓库污染时立即停止写操作；

发现硬件低于要求时完成审计，不得自行购买、升级或切换平台；

不得为了获得理想状态删除失败日志或编造成功输出。

十一、最终回报模板

聊天中必须原样按以下结构返回，不要只回复“已完成”。

1. 最终执行状态
SUBMITTED_S0_INITIALIZATION
或
SUBMITTED_S0_WITH_LIMITATIONS
或对应 BLOCKED 状态

2. 项目与仓库
本地根目录：
GitHub 仓库：
main 基线 commit（完整 40 位）：
执行分支：
EVIDENCE_COMMIT（完整 40 位）：
SUBMISSION_COMMIT（完整 40 位）：
Draft PR：<URL 或 NOT_CREATED>

3. 工作区边界
是否存在父级 Git 污染：
初始文件清单：
00 总纲前后 SHA256 是否一致：

4. 硬件实测
操作系统：
CPU：
物理核心/逻辑线程：
内存：
GPU：
显存：
NVIDIA 驱动：
驱动报告 CUDA 兼容版本：
本地 CUDA Toolkit：
C 盘剩余：
D 盘剩余：

5. WSL 与工具链
WSL2：
Linux 发行版：
Python：
Conda：
Git：
gh：
CMake/Ninja：
LongPathsEnabled：

6. 第三方冻结
AM-Planner HEAD：
AM-Planner 许可证：
Isaac Lab v2.3.2 commit：
Isaac Lab 许可证：
Polynomial_DiT HEAD：
Polynomial_DiT 许可证：

7. 环境策略结论
AM-Planner 候选运行方式：
Isaac Lab 候选运行方式：
两者是否隔离：
当前是否安装：均未安装

8. 已创建或修改的关键文件
<列出路径>

9. 已运行检查
<命令、退出码、关键结果>

10. 风险与限制
<按严重程度排序>

11. 明确未执行事项
未安装 AM-Planner；
未安装 Polynomial_DiT checkpoint；
未安装 Isaac Sim/Isaac Lab；
未修改驱动、WSL 或系统配置；
未启动任何训练或正式实验；
未合并执行分支到 main。

12. 请求高级总控审查
请高级总控从 GitHub 核验 main 基线、执行分支差异、证据文件和测试结果，再决定 S0 是否通过。
