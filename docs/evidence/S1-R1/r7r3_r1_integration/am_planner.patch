--- a/src/plan/traj_opt/CMakeLists.txt
+++ b/src/plan/traj_opt/CMakeLists.txt
@@ -2,12 +2,13 @@
 project(traj_opt)
 
 set(CMAKE_BUILD_TYPE "Release")
-ADD_COMPILE_OPTIONS(-std=c++14 )
+add_compile_options(-std=c++14)
 set(CMAKE_CXX_FLAGS_RELEASE "-O3 -Wall -g")
 
+find_package(Python3 3.9 EXACT REQUIRED COMPONENTS Interpreter Development)
+find_package(pybind11 CONFIG REQUIRED)
 find_package(Eigen3 REQUIRED)
 find_package(octomap REQUIRED)
-find_package(pybind11 REQUIRED)
 find_package(autodiff REQUIRED)
 find_package(catkin REQUIRED COMPONENTS
   roscpp
@@ -22,10 +23,16 @@
   message_generation
 )
 
-include_directories( 
-    SYSTEM 
-    include 
-    ${catkin_INCLUDE_DIRS} 
+# Filter only Python 3.8 from a local Catkin library copy; preserve ROS libs.
+set(_traj_opt_catkin_libraries ${catkin_LIBRARIES})
+list(FILTER _traj_opt_catkin_libraries EXCLUDE REGEX "python3\\.8")
+list(APPEND _traj_opt_catkin_libraries Python3::Python)
+
+include_directories(
+    SYSTEM
+    include
+    ${catkin_INCLUDE_DIRS}
+    ${Python3_INCLUDE_DIRS}
     ${pybind11_INCLUDE_DIRS}
 )
 
@@ -44,7 +51,9 @@
     src/se3_planner.cc
 )
 
+target_include_directories(se3_planner PRIVATE ${Python3_INCLUDE_DIRS})
 target_link_libraries(se3_planner
-    ${catkin_LIBRARIES}
+    ${_traj_opt_catkin_libraries}
+    pybind11::embed
     autodiff::autodiff
 )
--- a/src/plan/plan_manage/CMakeLists.txt
+++ b/src/plan/plan_manage/CMakeLists.txt
@@ -1,29 +1,18 @@
 cmake_minimum_required(VERSION 3.10.0)
 project(plan_manage)
+
 set(CMAKE_BUILD_TYPE "Release")
-ADD_COMPILE_OPTIONS(-std=c++14 )
+add_compile_options(-std=c++14)
 set(CMAKE_CXX_FLAGS_RELEASE "-O3 -Wall -g")
 
-# 使用现代CMake的方式自动查找Python
-# 这会利用`conda activate`设置的环境变量，自动找到正确的Python
-find_package(Python 3 REQUIRED COMPONENTS Interpreter Development)
-message(STATUS "Found Python executable: ${Python_EXECUTABLE}")
-message(STATUS "Found Python include dirs: ${Python_INCLUDE_DIRS}")
-message(STATUS "Found Python libraries: ${Python_LIBRARIES}")
-
-# 查找pybind11。它会自动使用上面找到的Python版本
-find_package(pybind11 REQUIRED)
-
+find_package(Python3 3.9 EXACT REQUIRED COMPONENTS Interpreter Development)
+message(STATUS "Found Python executable: ${Python3_EXECUTABLE}")
+message(STATUS "Found Python include dirs: ${Python3_INCLUDE_DIRS}")
+message(STATUS "Found Python libraries: ${Python3_LIBRARIES}")
+find_package(pybind11 CONFIG REQUIRED)
 
 find_package(Eigen3 REQUIRED)
 find_package(Boost REQUIRED COMPONENTS system filesystem)
-
-# 设置pybind11查找参数
-set(PYBIND11_PYTHON_VERSION "3" CACHE STRING "")
-set(PYTHON_EXECUTABLE ${PYTHON_EXECUTABLE})
-
-# 添加pybind11查找
-find_package(pybind11 REQUIRED)
 find_package(autodiff REQUIRED)
 
 find_package(catkin REQUIRED COMPONENTS
@@ -34,12 +23,17 @@
   traj_opt
 )
 
-include_directories( 
-    SYSTEM 
-    include 
-    ${catkin_INCLUDE_DIRS} 
+# Filter only Python 3.8 from a local Catkin library copy; preserve ROS libs.
+set(_plan_manage_catkin_libraries ${catkin_LIBRARIES})
+list(FILTER _plan_manage_catkin_libraries EXCLUDE REGEX "python3\\.8")
+list(APPEND _plan_manage_catkin_libraries Python3::Python)
+
+include_directories(
+    SYSTEM
+    include
+    ${catkin_INCLUDE_DIRS}
     ${EIGEN3_INCLUDE_DIRS}
-    ${PYTHON_INCLUDE_DIRS}
+    ${Python3_INCLUDE_DIRS}
     ${pybind11_INCLUDE_DIRS}
 )
 
@@ -49,11 +43,9 @@
     src/plan_manage.cpp
 )
 
+target_include_directories(se3_node PRIVATE ${Python3_INCLUDE_DIRS})
 target_link_libraries(se3_node
-  ${catkin_LIBRARIES}
+  ${_plan_manage_catkin_libraries}
   pybind11::embed
   autodiff::autodiff
 )
-
-# 确保编译时能找到Python.h
-target_include_directories(se3_node PRIVATE ${PYTHON_INCLUDE_DIRS})
\ No newline at end of file
